# Clean Deploy Branch

Esta rama contiene el código del proyecto sin el archivo grande `Backend/cronodemo.db`. Se creó para permitir el despliegue en Render.com sin superar los límites de tamaño del repositorio.
